#include <stdio.h>

int main() {
    int n, m, i;
    printf("Podaj dwie liczby calkowite n i m: ");
    scanf("%d %d", &n, &m);
    
    for (i = n; i < m; i += n) {
        printf("%d\n", i);
    }
    
    return 0;
}
