#include <stdio.h>

int main() {
    int n, i;
    unsigned long long factorial = 1;
    
    printf("Podaj nieujemna liczbe calkowita n: ");
    scanf("%d", &n);
    
    for (i = 1; i <= n; i++) {
        factorial *= i;
    }
    
    printf("%d! = %llu\n", n, factorial);
    
    return 0;
}
