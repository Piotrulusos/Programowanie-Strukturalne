#include <stdio.h>

int main() {
    int n, m, k, i, multiple;
    printf("Podaj trzy liczby calkowite n, m i k: ");
    scanf("%d %d %d", &n, &m, &k);
    
    for (i = m+1; i < k; i++) {
        if (i % n == 0) {
            printf("%d\n", i);
        }
    }
    
    return 0;
}
